{
  "private": true,
  "name": "bento-editor",
  "version": "0.0.1",
  "volta": {
    "node": "16.14.2"
  },
  "license": "MIT",
  "scripts": {
    "preflight": "npm ci .",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "workspaces/*",
    "examples/*"
  ],
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "jest": "^28.1.1",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.3"
  }
}
