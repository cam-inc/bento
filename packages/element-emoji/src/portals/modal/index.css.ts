import { style } from '@vanilla-extract/css';

export const styles = {
  root: style({
    pointerEvents: 'auto',
    position: 'absolute',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
  }),
};
